[merge]
    tool = vscode
    conflictstyle = diff3
    keepBackup = false

[mergetool "vscode"]
    cmd = code --wait $MERGED

[pull]
    rebase = false

[core]
    autocrlf = true
    editor = code --wait
