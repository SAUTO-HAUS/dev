<?php defined( '_DOIT' ) or die( 'Restricted access' );

	/*$query_args = array();
	$sql = 'SELECT * FROM '.$prefx.'_seo';
		
	$pdo = $db->prepare($sql);
	$pdo->execute($query_args);
	
	foreach ($pdo as $row){
		foreach($arr_types as $key){
			if($row[$key]!=''){ $cars_arr[$key][] = $row[$key]; }
		}
	}*/

?>